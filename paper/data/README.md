# Measurement data

Raw benchmark output is intentionally not committed until it is accompanied by
complete machine metadata and a frozen source revision. See
[`../experiments.md`](../experiments.md) for the required directory structure,
collection protocol, and result gates.

Software-renderer output may be retained under a directory clearly named
`correctness-only`, but it must not feed performance figures.
